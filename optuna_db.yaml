# SQLite — no server needed, file is created automatically on first use.
# Path is inside the container (/ws = $WS on the host).
# For PostgreSQL instead, replace with individual fields:
#   db_user, db_password, db_host, db_port, db_name, sslmode
url: sqlite:////ws/optuna/optuna.db
